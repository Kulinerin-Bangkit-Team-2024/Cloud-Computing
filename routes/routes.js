const express = require("express");
const router = express.Router();
const userConstroller = require("../controllers/user")
const foodConstroller = require("../controllers/auth");
const authConstroller = require("../controllers/food");

//Auth

//User

//Food
